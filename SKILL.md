---
name: card-maker
description: |
  将文本、数据、网页内容转化为精美 HTML 信息卡片并导出为 PNG 图片。
  支持竖版（小红书 1080×1440）、横版（1280×自适应）、插图版（1280×720）三种尺寸。
  内置两步走工作流：先输出大纲审核，确认后生成 HTML 并截图。
  支持主题配色自适应（可从参考图片提取配色）和品牌信息自定义。
  当用户要求"生成卡片"、"排版"、"信息图"、"海报"、"做张图"、"card"、"infographic"时使用。
---

# Card Maker - HTML 信息卡片生成器

将文本/数据/网页内容提炼后，生成精美 HTML 卡片并截图导出 PNG。

## 工作流

### 阶段一：内容提炼与大纲（必须先输出，等用户确认）

**禁止直接生成 HTML**。必须先提炼内容并输出大纲。

1. **输入处理**：用户给 URL 则先用 `web_fetch` 抓取内容，给纯文本则直接提炼。
2. **提炼规则**：
   - 只保留"删掉就损失信息"的核心判断与数据
   - 提取具体数字、倍率、年份、金额、对比关系、因果链
   - 硬核干货绝不压缩指标，通过增加卡片页数承载
   - 主标题必须是明确的结论或洞察，优先用动词/数字/反差
   - 金句必须来自原文，不捏造
3. **排版推演**：
   - 提取 1 个主关键词 + 1-3 个副关键词
   - 预估阅读时长（通常 30 秒内），填入 TLDR 栏（插图版无此项）
   - 竖版容量法则：单页最多 "1 个大标题 + 3 个段落模块"，超出必须分页
   - 3 个以上同类对比数据必须计划 ECharts 图表
4. **输出大纲模板**：

```markdown
### 📝 提炼大纲与排版计划

**【卡片基础信息】**
- **主标题**：（反差/冲突结论）
- **关键词**：[主] [副1] [副2]
- **TLDR**：XX秒速览（插图版无此项）

**【核心内容模块】**
- **模块 1**：[小标题] - [核心内容]
- **模块 2**：[小标题] - [核心内容]

**【金句】**
- "（原文金句）"

**【排版与图表规划】**
- 分页策略：X 页竖版 / 横版单页
- 图表策略：纯图文 / 第X模块用 ECharts 绘制【图表类型】

💡 *确认后回复"确认"，我将生成 HTML。*
```

### 阶段二：HTML 生成与截图（确认后执行）

1. 严格按大纲生成 HTML，不篡改核心意思
2. 如有 ECharts 图表：禁用动画（`animation: false`）、禁用交互、用调色盘配色、数值直标
3. 竖版每页锁定 1440px 高度，溢出放入新页
4. 内容过少时：弹性均分 / 放大字号 / 金句填充，禁止单纯 mt-auto 推底
5. 底部留白 >300px 时插入装饰 SVG 或大金句版块
6. 最后模块与落款分割线间必须 mb-8 以上
7. 生成后用 Playwright 截图导出 PNG，参见截图脚本说明

## 尺寸规格

| 版式 | 宽度 | 高度 | 适用场景 |
|---|---|---|---|
| 竖版 | 1080px | 1440px（锁定） | 小红书轮播卡片 |
| 横版 | 1280px | 自适应（最低 720px） | X/Twitter 长图、小绿书 |
| 插图版 | 1280px | 720px（锁定） | 公众号封面、博客题图 |

开始前必须询问用户选哪种尺寸（如未指定）。

## 主题配色

**默认**：深色科技风（深蓝紫底 + 紫/青渐变高光）。详见 [references/themes.md](references/themes.md)。

**自适应**：如用户提供参考图片，从图片提取主色调并映射到卡片配色。提取规则：
1. 识别图片主背景色 → 卡片底色
2. 识别图片高光/强调色 → 标签、标题高亮、分割线
3. 识别图片文字色 → 正文、辅助文字
4. 输出配色方案供用户确认后再生成

用户也可直接指定风格关键词（复古/科技/极简/商务等）。

## 品牌信息

默认使用 🦞 OpenClaw 品牌。用户可自定义：
- Logo 图标/文字
- 品牌名称
- 落款署名

在生成前确认品牌信息，替换模板中对应位置。

## ECharts 使用规范

1. 引入：`<script src="https://cdn.jsdelivr.net/npm/echarts@5/dist/echarts.min.js"></script>`
2. 必须设置 `animation: false`，禁用 tooltip/hover
3. 配色使用当前主题调色盘，背景透明
4. 通过 `label` 直标数值，因无交互提示框
5. 图表必须在页面加载后初始化，截图前等待渲染完成

## 截图导出

HTML 生成后，使用 Playwright 截图。详见 [references/screenshot.md](references/screenshot.md)。

关键步骤：
1. 将 HTML 写入 `%TEMP%/card-maker-<keyword>.html`
2. 用 Playwright 打开文件，等待 networkidle + 额外 3 秒
3. 定位 `.export-card` 元素逐个截图
4. 输出 PNG 到 `%TEMP%/card-<keyword>-1.png` 等
5. 在浏览器中打开结果供预览

## 文件输出规则

1. 仅输出单个完整 HTML 文件，内联 CSS
2. 禁止任何保存图片的 JS 代码或按钮
3. 禁止交互元素（动画/hover/tooltip）
4. HTML 保存至 `%TEMP%/card-maker-<keyword>.html`
5. 截图保存至 `%TEMP%/card-<keyword>-<n>.png`
